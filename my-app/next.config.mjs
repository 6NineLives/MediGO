/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['news.llu.edu','i0.wp.com'], // Add allowed external domains here
    },
  };
  
  export default nextConfig;
  